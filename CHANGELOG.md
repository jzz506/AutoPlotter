# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-07-19

### 新增

- **Web Worker 解析**：CSV / TXT / Excel 解析移入后台 Worker 线程，大文件不再阻塞界面；解析进度实时显示；新增“取消解析”按钮。
- **误差棒**：折线图 / 散点图 / 柱状图 / 水平柱状图支持对称误差列、上下分别指定的双误差列，以及按 X 分组自动计算的标准差（SD）与标准误（SEM）；散点图使用自动 SD/SEM 时给出明确解释。
- **绘图预设**：论文图（Arial、紧凑尺寸）、汇报图（大字号、16:9）、网页图（默认）。
- **对数坐标**：X / Y 轴独立开启；非正值数量在图表上方明确提示。
- **参考线与文本标注**：可添加多条水平 / 垂直参考线（带标签）与相对坐标文本标注。
- **可复现 Python 脚本**同步支持以上全部新配置，语义与网页一致（已真机执行验证：误差棒、参考线、标注、预设字体均正确生成）。

### 改进

- 采样、类别截断、热图列数限制、颜色分组合并等所有数据降级行为均在图表上方明确提示，无静默处理（含单元测试覆盖）。

### 性能实测（Apple Silicon，生产构建）

| 数据规模 | 解析时间 | 绘图时间（含推荐与渲染） | 降级提示 |
|---|---|---|---|
| 10 万行 × 6 列（4.3 MB） | ~0.7 s | ~0.4 s | 均匀采样 20,000 / 100,000 行（有提示） |
| 50 万行 × 6 列（21.6 MB） | ~3.4 s | ~1.2 s | 均匀采样 20,000 / 500,000 行（有提示） |

压力测试数据由 `scripts/make-stress-data.mjs` 生成（不入库），测量命令：`npm run test:perf`（需先启动 `vite preview` 于 4173 端口）。峰值风险：Worker 与主线程间结构化克隆会短暂翻倍数据内存占用；50 万行级别在常见机型上稳定，超过 50 万行按上限截断并警告。

## [1.0.0] - 2026-07-18

首个正式发布版本。

### 功能

- **文件导入**：拖拽 / 点击上传 CSV、XLSX、XLS、TXT；自动识别逗号 / 分号 / 制表符分隔符；UTF-8（含 BOM）、UTF-16 LE/BE、GB18030 编码处理；Excel 多工作表选择与切换；解析进度显示；文件名、行数、列数展示；分页预览；损坏文件、不支持格式、超大文件（>100 MB 拒绝，>20 MB 提示，>50 万行截断）的明确提示。
- **数据概览**：自动推断列类型（数值 / 类别文本 / 日期时间 / 布尔值 / 无法识别）；展示非空数、缺失数与比例、唯一值数、数值列统计（最小 / 最大 / 均值 / 中位数 / 标准差）、文本高频值、日期起止。
- **质量报告**：缺失值、重复行、空列名、重复列名、常数列、疑似 ID 列、IQR 异常值、文本形式数字、日期解析失败、高基数类别列；只报告不修改原始数据。
- **数据处理**：删除缺失行、均值 / 中位数 / 众数填补、删除重复行、文本转数值、转日期、保留列、排序、类别筛选、数值范围筛选、逐步撤销、一键恢复原始数据；全部操作仅作用于内存副本。
- **图表推荐**：确定性本地规则推荐（折线、散点、柱状、箱线、直方图、频数柱状、相关性热图、多序列折线），附推荐理由，一键采用。
- **手动绘图**：折线 / 散点 / 柱状 / 水平柱状 / 直方图 / 箱线 / 小提琴 / 饼图 / 相关性热图共 9 种；可调整 X/Y 轴、颜色分组、聚合、排序、标题、轴名、图例、网格、宽高、字号、主题；类型不兼容时给出中文解释，不生成误导性图表；采样、截断、类别合并等数据降级均在图表上方明确提示。
- **导出**：PNG、SVG、独立可交互 HTML（内嵌 Plotly，离线可打开）、处理后 CSV / XLSX、图表配置 JSON。
- **可复现 Python 脚本**：根据当前处理步骤与绘图配置生成可直接运行的 pandas + plotly 脚本（读取 → 处理 → 聚合 → 绘图 → 保存），与网页端结果数值级一致（已自动化验证）。
- **桌面应用**：基于 Tauri 2 的原生 macOS 应用（Apple Silicon），支持从 Finder 拖拽文件到窗口，导出使用原生保存对话框；提供 `.app` 与 `.dmg` 产物，无需 Apple Developer 账号，未签名未公证。
- **隐私**：全部数据仅在本机（浏览器 / WebView 内存）处理，无后端、无任何网络上传。

### 已知限制

- `xlsx@0.18.5`（SheetJS 的 npm 最后版本）存在已知安全公告；应用纯本地解析用户自选文件，风险有限。
- 折线图 / 散点图超过 2 万数据点时自动均匀采样；柱状图类别超过 50 个时按当前排序截断；饼图类别超过 30 个时合并为「其他」（前 20 项）；散点颜色分组超过 20 类时合并为「其他」；相关性热图最多使用前 15 个数值列。以上均有界面提示。
- 文件超过 50 万行时仅加载前 50 万行（导入时有明确警告）。
- 桌面应用未做 Apple 签名与公证，首次打开需在 系统设置 → 隐私与安全性 允许，或右键 → 打开。
- 导出的独立 HTML 文件约 5 MB（内嵌完整 Plotly 运行库）。
- GB18030 编码检测基于 UTF-8 解码失败回退，极端混合编码文件可能识别不准。
